fastapi
uvicorn[standard]

# ORM / DB
SQLAlchemy>=2
psycopg[binary]>=3.1
# If you are NOT using Turso/libsql anymore, remove the next line:
# sqlalchemy-libsql

# Pydantic + email validation
pydantic[email]>=2.0
pydantic-settings

# Auth / crypto
passlib[bcrypt]>=1.7.4
bcrypt==3.2.2
python-jose[cryptography]>=3.3.0

# HTTP / uploads
httpx
python-multipart

# Observability / OpenAI agents (keep if you use them)
openai-agents
langsmith[openai-agents]
